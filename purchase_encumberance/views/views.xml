<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="purchase_encumberance.list">
      <field name="name">purchase_encumberance list</field>
      <field name="model">purchase_encumberance.purchase_encumberance</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="purchase_encumberance.action_window">
      <field name="name">purchase_encumberance window</field>
      <field name="res_model">purchase_encumberance.purchase_encumberance</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="purchase_encumberance.action_server">
      <field name="name">purchase_encumberance server</field>
      <field name="model_id" ref="model_purchase_encumberance_purchase_encumberance"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="purchase_encumberance" id="purchase_encumberance.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="purchase_encumberance.menu_1" parent="purchase_encumberance.menu_root"/>
    <menuitem name="Menu 2" id="purchase_encumberance.menu_2" parent="purchase_encumberance.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="purchase_encumberance.menu_1_list" parent="purchase_encumberance.menu_1"
              action="purchase_encumberance.action_window"/>
    <menuitem name="Server to list" id="purchase_encumberance" parent="purchase_encumberance.menu_2"
              action="purchase_encumberance.action_server"/>
    -->
  </data>
</odoo>